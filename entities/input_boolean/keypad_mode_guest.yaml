---
# Input boolean: Guest Keypad Mode
#
# This mode is to set the keypads into night mode.
name: "Guest Keypad Mode"
#icon: >
#  if (state === 'on') return 'mdi:weather-night';
#  return 'mdi:weather-sunny'
